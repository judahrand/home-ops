machine:
  sysctls:
    vm.nr_hugepages: "1024"
  kubelet:
    extraMounts:
      - destination: /var/local/openebs
        options:
          - bind
          - rshared
          - rw
        source: /var/local/openebs
        type: bind
