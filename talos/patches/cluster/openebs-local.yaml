machine:
  kubelet:
    extraMounts:
      - destination: /var/local/openebs
        options:
          - bind
          - rshared
          - rw
        source: /var/local/openebs
        type: bind
